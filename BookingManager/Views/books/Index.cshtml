@model IEnumerable<BookingManager.Models.book>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
          Title
        </th>
        <th>
            Auther
        </th>

        <th>
            Main Catogory
        </th>
        <th>
            Sub atogory
        </th>
        <th>
           Created at
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.title)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.auther)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.sub_catrogoris.catogory.name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.sub_catrogoris.name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.created_at)
    </td>

    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.book_id }) |
        @Html.ActionLink("Details", "Details", new { id = item.book_id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.book_id })
    </td>
</tr>
}

</table>
