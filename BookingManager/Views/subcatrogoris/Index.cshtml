@model IEnumerable<BookingManager.Models.sub_catrogoris>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.main_category)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.catogory.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.main_category)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.catogory.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.subCatogory_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.subCatogory_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.subCatogory_id })
        </td>
    </tr>
}

</table>
